server:
  port: ${PORT:8080}


spring:
  main:
    banner-mode: off

datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password:


jpa:
    generate-ddl: true
    hibernate.ddl-auto: create
    show-sql: true